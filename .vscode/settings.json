// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "**/*.js": true,
        "**/*.map": true,
        "**/*LICENSE": true,
        "**/*.md": true,
        "**/*lock.json": true
    }
}